# 新线程交接提示词

请直接复制下面整段内容到新的 Codex 线程：

```text
请在当前本地工程中直接实现「抖音扫码导入喜欢内容 → 兴趣画像 → 选择题校准 → 最终个人标签」的后端 MVP，并完成测试与本地真实演示。不要只写设计方案。

工程根目录：
/Users/baihe/Documents/compusone

完整接口规格：
/Users/baihe/Documents/compusone/docs/07_抖音兴趣标签导入接口.md

请先完整阅读：
1. /Users/baihe/Documents/compusone/README.md
2. /Users/baihe/Documents/compusone/docs/00_产品方案_V2.1.md
3. /Users/baihe/Documents/compusone/docs/07_抖音兴趣标签导入接口.md
4. /Users/baihe/Documents/compusone/onemore/modules/profile/api.py
5. /Users/baihe/Documents/compusone/onemore/modules/profile/service.py
6. /Users/baihe/Documents/compusone/onemore/db/models.py
7. /Users/baihe/Documents/compusone/onemore/hermes/login.py
8. /Users/baihe/Documents/compusone/tests/conftest.py

产品与实现边界：
- 这是黑客松 MVP，不接抖音开放平台，不使用 ClientKey、ClientSecret、OAuth 应用审核或复杂上架流程。
- 使用已经跑通的实践路径：独立 Chrome/Playwright 会话展示抖音登录二维码，用户扫码后捕获该隔离会话的网页登录状态，再从喜欢页采集数据。
- ONE MORE 自己现有的 X-User-ID/Bearer 用户鉴权必须保留；“不认证”仅指不接抖音开放平台审核体系。
- 标签默认私有，不做公开社交主页，不把抖音推断结果写入课程产生的 verified_tags。
- Cookie 只保存在任务隔离目录中，禁止通过 API 返回、写进日志或进入最终画像；任务结束、取消、失败后清理。
- 当前工作区没有 Xcode/SwiftUI 源码，因此本线程先完成 FastAPI 后端、OpenAPI、测试和真实浏览器演示。

已验证的参考实现位于：
/Users/baihe/Documents/compusone/douyin_like_profile/DouK-Downloader/scripts/capture_login.py
/Users/baihe/Documents/compusone/douyin_like_profile/DouK-Downloader/scripts/collect_browser_full.py
/Users/baihe/Documents/compusone/douyin_like_profile/DouK-Downloader/scripts/build_dataset.py
/Users/baihe/Documents/compusone/douyin_like_profile/DouK-Downloader/scripts/analyze_profile.py

这套脚本已经真实跑通：153 个 favorite API 页面、2,383 条唯一喜欢作品，末页 has_more=0。请复用其核心逻辑，但重构到 ONE MORE 模块中；不要从脚本 shell out，也不要把 DouK-Downloader 当运行时服务依赖。DouK 的纯 HTTP 终端路径实测只拿到 77 条后失败，所以真实 Provider 必须以浏览器响应拦截为主。

请实现以下模块结构（可在保持职责清楚的前提下微调）：
onemore/modules/taste_profile/
  __init__.py
  api.py
  schemas.py
  service.py
  analyzer.py
  taxonomy.py
  orchestrator.py
  providers/
    __init__.py
    base.py
    fake.py
    douyin_browser.py

必须实现的接口：
- POST /profile/imports/douyin
- GET /profile/imports/{import_id}
- POST /profile/imports/{import_id}/qr/refresh
- POST /profile/imports/{import_id}/cancel
- GET /profile/imports/{import_id}/items?cursor=0&limit=50
- GET /profile/imports/{import_id}/questions
- POST /profile/imports/{import_id}/answers
- GET /profile/taste/me
- DELETE /profile/taste/me/douyin

同时在 GET /profile/me 中增加向后兼容的可选 taste_profile 摘要字段，但不要改写 verified_tags、self_reported_tags 或 capability_vector。

状态机必须包括：
PREPARING_QR → WAITING_SCAN → AUTHENTICATED → RESOLVING_PROFILE → COLLECTING → ANALYZING → NEEDS_CONFIRMATION → READY
以及 QR_EXPIRED、FAILED、CANCELLED。

真实浏览器 Provider 要求：
1. 每个 import_id 使用独立 runtime/douyin/{import_id} 目录和独立浏览器 Context。
2. 使用随机空闲 CDP 端口或 Playwright persistent context，不要固定共用 9223。
3. 打开抖音登录页、触发登录弹窗、捕获二维码并发布为 qr_image_data_url。
4. 检测 sessionid/sessionid_ss/sid_tt/sid_guard 等登录态 Cookie，但绝不输出其值。
5. 登录后自动解析当前用户 sec_uid；优先当前用户接口/页面状态，其次头像的 /user/{sec_uid} 链接，最后才使用创建请求的可选 profile_url 兜底。
6. 打开 https://www.douyin.com/user/{sec_uid}?showTab=like。
7. 监听 /aweme/v1/web/aweme/favorite/ 响应，以 aweme_list 为权威数据、aweme_id 去重。
8. 不得把 DOM 中推荐位或侧栏的 DOM-only 链接混入喜欢列表。
9. 滚动最大可滚动容器，直到捕获 has_more=0；每页更新 api_pages 和 items_collected。
10. 支持二维码过期刷新、用户取消、总超时和稳定轮次兜底。
11. 所有终态都关闭浏览器并清理临时登录状态。

后台任务不能阻塞 HTTP 创建请求。本地黑客松模式可使用进程内 ThreadPoolExecutor(max_workers=2)，每个任务自行创建 SessionLocal；保留未来 Celery 的扩展点即可。参考 onemore/hermes/login.py 的长任务模式。进程启动时把遗留非终态任务标为 FAILED/IMPORT_WORKER_RESTARTED。

新增配置并更新 .env.example：
ONEMORE_DOUYIN_IMPORT_ENABLED
ONEMORE_DOUYIN_MODE=fake|browser
ONEMORE_DOUYIN_BROWSER_EXECUTABLE
ONEMORE_DOUYIN_RUNTIME_ROOT
ONEMORE_DOUYIN_QR_TIMEOUT_SECONDS
ONEMORE_DOUYIN_COLLECTION_TIMEOUT_SECONDS
ONEMORE_DOUYIN_MAX_PARALLEL_IMPORTS
ONEMORE_DOUYIN_KEEP_RAW_DEBUG

新增 SQLAlchemy 模型与 Alembic 迁移：TasteImportSession、TasteProfile。原始规范化条目在 MVP 中优先保存为任务 JSONL，数据库保存聚合结果、问题、答案和最终画像。runtime、Cookie、Chrome profile 和原始文件必须加入 .gitignore。

标签算法要求：
- 复用参考 analyze_profile.py 的主题分类、行为信号和作者广度统计，重构成可测试的纯函数。
- 最近 200 条按喜欢列表顺序作为近期窗口；不要把作品 create_time 当点赞时间。
- 至少支持：探索型 Builder、AI 实践派、务实浪漫、策略玩家、知识策展人、审美观察者、成长驱动者。
- 根据最不确定的维度从固定题库选择 3–5 道单选题。
- 样本 >= 200 时 final = content*0.70 + quiz*0.30；样本较少时各 0.50。
- 输出一个主标签、2–3 个副标签、兴趣领域、维度分数、一句话摘要、confidence、sample_summary 和 model_version=taste-v1。
- MVP 不依赖外部 LLM/API，结果必须确定性、可测试。

测试要求：
- 新增 tests/test_douyin_taste_import.py。
- fake provider 覆盖完整状态机，不需要真实扫码就能让自动化测试完成。
- 覆盖二维码刷新、任务所有权、取消、aweme_id 去重、has_more=0、3–5 道题、非法答案、READY 结果、/profile/me 摘要、删除画像以及 Cookie 不出现在 API/日志/数据库中。
- 运行并修复 uv run ruff check onemore tests migrations、uv run mypy onemore、uv run pytest。
- 运行 uv run onemore-export-openapi，更新 openapi/onemore.openapi.json。
- 不得破坏现有测试和产品红线。

实施顺序：
1. 先检查当前工程与依赖，建立实现计划。
2. 完成模型、迁移、schemas、service、fake provider、analyzer 和 API。
3. 跑完所有自动化测试。
4. 再实现 douyin_browser Provider。
5. 启动 ONEMORE_DOUYIN_MODE=browser 的本地服务，创建真实导入任务并把新二维码展示给我扫码。
6. 扫码后跑到 has_more=0，提交校准答案并验证最终标签。
7. 最终给出改动文件、测试结果、真实采集结果和客户端接入顺序。

不要停在方案或伪代码；直接修改工程并持续执行到自动化测试通过。遇到真实扫码时再明确展示二维码并等待我确认。
```
