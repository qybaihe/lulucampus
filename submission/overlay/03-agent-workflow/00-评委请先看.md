# Agent / 核心 Workflow · 噜噜成局

本包只放编排层：两个 Agent、四条工作流、成局状态机。完整可运行工程在 `04-Source`。

先读 `WORKFLOWS.md`。

## 两个 Agent（物理分离）

| | hermes | 噜噜 / Edge 编排 |
|---|---|---|
| 目录 | `hermes/` | `edge-agent/` |
| 服务谁 | 一个人 | 一个局 / 一轮对话 |
| 原则 | 越常用越好 | 办完就退场 |
| 凭证 | 服务端 Vault | 留在客户端，云端不落盘 |

## 目录

```
WORKFLOWS.md                 四条链 + 状态机
hermes/                      Action Schema、Campus MCP、执行器、sidecar
edge-agent/                  EdgeOne 校园编排（OpenAI Agents SDK）
workflows/                   成局 / 意图 / 匹配 / 校园动作 / 预约确认
openapi/onemore.openapi.json 前端契约
```

Hermes 写操作铁律：预览 → 确认 → 执行。客户端传来的 `confirm=true` 一律丢弃。
